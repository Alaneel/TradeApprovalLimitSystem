spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/trading_system_test

  redis:
    host: localhost
    port: 6379

logging:
  level:
    root: INFO
    com.gic: DEBUG

server:
  port: 0
